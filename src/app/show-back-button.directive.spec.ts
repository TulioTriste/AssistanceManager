import { ShowBackButtonDirective } from './show-back-button.directive';

describe('ShowBackButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowBackButtonDirective();
    expect(directive).toBeTruthy();
  });
});
